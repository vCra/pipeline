class Manager(object):
    module = None

    def update_config(self, configuration) -> dict:
        """
        Apply's this managers configuration for this particular job
        :param configuration:
        :return:
        """
        return configuration
